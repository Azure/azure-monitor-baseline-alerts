# logo image, direction and title
logo = { src = '/images/logo.png' } # a global resource called `/images/logo.<TYPE>` will be selected automatically; nevertheless if you want to color an SVG it has to be inlined and so must be set manually here
#logo = {src = 'img/amba_logo_thumb.png'}
direction = 'column'
linkTitle = "Azure\nMonitor\nBaseline\nAlerts"

# menu and submenu settings
alwaysopen = false
collapsibleMenu=true

# landing page
disableLandingPageButton = true

# page meta information
hideAuthorName = true
hideAuthorDate = true
hideAuthorEmail = true
